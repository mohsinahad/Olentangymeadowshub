# Olentangy Meadows

A neighborhood summer services marketplace built with Next.js, Prisma (SQLite), NextAuth, Pusher, and Stripe.

## Setup

```bash
npm install
cp .env.example .env  # fill in your values
npx prisma migrate dev
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
